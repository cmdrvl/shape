# Ambition Bar Check

The pass applied four high-leverage changes to a small CLI that already had nested doctor surfaces:

- top-level mega-command: `shape --robot-triage`
- top-level capability contract: `shape capabilities --json`
- top-level agent guide: `shape robot-docs guide`
- safe refusal for `shape doctor --fix`
- global JSON intent for doctor health

The "That's it??" self-prompt was run mentally before ending the pass. The remaining deferred class is generalized typo correction for arbitrary flags; this pass focused on making the first commands an agent naturally tries work directly.

